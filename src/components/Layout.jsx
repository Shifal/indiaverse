const Layout = ({ children }) => {
    return (
      <div className="min-h-screen bg-gradient-to-br from-blue-50 to-black text-gray-100">
        {children}
      </div>
    );
  };
  
  export default Layout;
  